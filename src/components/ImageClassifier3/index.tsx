import * as React from 'react';
import styled from 'styled-components';

const Span = styled.span`
  color: green;
`;

const ImageClassifier = () => (
  <Span>Image Classifier goes here</Span>
);

export default ImageClassifier;
